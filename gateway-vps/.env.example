# --- General Configuration ---
# Your main domain (e.g., kylehub.dev)
DOMAIN_ROOT=example.com

# Subdomain for Pangolin (e.g., pangolin)
# Full URL will be: https://${PANGOLIN_SUBDOMAIN}.${DOMAIN_ROOT}
PANGOLIN_SUBDOMAIN=pangolin

# The Full Dashboard URL (Required by Pangolin)
PANGOLIN_DASHBOARD_URL=https://${PANGOLIN_SUBDOMAIN}.${DOMAIN_ROOT}

# Server Secret (Required)
# Generate one with: openssl rand -hex 32
PANGOLIN_SERVER_SECRET=change_me_to_a_secure_random_hex_string

# The Public IPv4 address of this VPS (Required for WireGuard/Gerbil)
# Run `curl ifconfig.io` to find this.
VPS_PUBLIC_IP=1.2.3.4

# --- SSL / Let's Encrypt ---
# Email for Let's Encrypt expiration notices
ACME_EMAIL=admin@example.com

# --- Cloudflare (Optional / If using DNS Challenge) ---
# Required if your server is behind Cloudflare Proxy (Orange Cloud)
CF_API_EMAIL=admin@example.com
CF_DNS_API_TOKEN=YourCloudflareApiToken

# --- Zitadel Configuration ---
ZITADEL_DOMAIN=auth.example.com
# Generate with: openssl rand -base64 32 | tr -d '\n' | cut -c1-32
ZITADEL_MASTERKEY=Your32CharLongKeyHereXyZ

# Database (Zitadel)
POSTGRES_USER=zitadel
POSTGRES_PASSWORD=VerySecurePassword
POSTGRES_DB=zitadel

# --- Pangolin / Gerbil Configuration ---
# Enterprise License Key
PANGOLIN_LICENSE_KEY=your-license-key-here

# WireGuard Ports (UDP)
WG_PORT_1=51820
WG_PORT_2=21820

# --- SMTP Email Configuration (Optional) ---
# For email functionality: invitations, password resets, 2FA notifications
# Requires Postfix SMTP relay on VPS host (see mail/README.md)
# If not configured, email features will be disabled
PANGOLIN_EMAIL_SMTP_PASS=your-smtp-password