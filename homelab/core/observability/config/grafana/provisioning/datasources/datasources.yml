apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://obs-loki:3100
    isDefault: true
    jsonData:
      maxLines: 1000

  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://obs-prometheus:9090
    jsonData:
      timeInterval: "15s"
